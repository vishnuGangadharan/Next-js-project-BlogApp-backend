export default interface UserData {
    _id:string
    name: string
    email: string
    Designation: string
    password: string
    profilePicture: string
    isBlocked: boolean;
    isAdmin: boolean

}